<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nicolai.mandatorycantine">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivityForside">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2ActivityMenu" />
        <activity android:name=".Main3ActivityRate" />
        <activity android:name=".PointsMenu" />
        <activity android:name=".Main3ActivityTakeAway" />
        <activity android:name=".Main3ActivityLogIn" />
        <activity android:name=".DishActivity" />
        <activity android:name=".AddUser"></activity>
    </application>

</manifest>